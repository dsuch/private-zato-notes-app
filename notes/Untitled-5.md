<ul><li><p><strong>sweeping</strong> of tmp files and others when the broker starts</p></li><li><p>Make it possible to <strong>pause and unpause</strong> schedued jobs</p></li><li><p>With datadog, make sure the old log entries from <strong>self.logger.info</strong> etc. are still visible in logs</p></li><li><p>With datadog, make sure <a target="_blank" rel="noopener noreferrer nofollow" href="http://self.logger.info">self.logger.info</a> also goes to datadog</p></li><li><p>No info in logs about services being hot-deployed</p></li><li><p><strong>add UI for env. variables</strong></p></li><li><p>add MQTT</p></li><li><p><strong>sweeping</strong> of tmp files and others when the broker starts</p></li><li><p>The existing codebase already <strong>leaks</strong> internal details everywhere - <code>format!("{}", err)</code> on lines 262, 672, 719, 732, etc.</p></li><li><p>add jeff like delivery by client-defined timestamps (i guess this is done?)</p></li><li><p>The existing codebase already <strong>leaks</strong> internal details everywhere - <code>format!("{}", err)</code> on lines 262, 672, 719, 732, etc.</p></li><li><p><a target="_blank" rel="noopener noreferrer nofollow" href="https://stackoverflow.com/questions/79619633/how-check-ibmq-message-already-pushed-to-to-avoid-duplicates-in-the-queue-in-jav">https://stackoverflow.com/questions/79619633/how-check-ibmq-message-already-pushed-to-to-avoid-duplicates-in-the-queue-in-jav </a>I am using IBMQ to publish messages. Before publishing a message, I want to check whether it has already been published and is currently in the queue. If the message is already in the queue, it should not be published again, and a message should be displayed indicating that it has already been published.</p></li><li><p><strong>add a dedicated management thread</strong></p></li><li><p>add jeff like delivery by client-defined timestamps (i guess this is done?)</p></li><li><p></p><pre><code class="language-rust">atomic_write</code></pre><pre><code class="language-rust">let _ = fs::remove_file(&amp;tmp_path);</code></pre></li></ul><p></p>