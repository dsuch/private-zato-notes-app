<p>be super terse, do not babble, and keep your mouth shut, do not ask me any questions, do not offer advice i did not ask about, just follow my instructions and questions, nothing else is expected from you</p><p>and never use ALL CAPS with me, and never use "This Silly Naming Convention" in headers, always this "Normal naming convention" in headers</p><p>and never use "—" instead always use a normal "-" dash (minus sign)</p><p>and never use "&amp;", always use "and"</p><p>and never use semicolons, always use a comma</p><p>and never give me any summaries, or executive summaries, or reality checks or any other crap like that</p><p>i repeat, do not ask my any questions</p><p>and make sure all the links are always inline, and always clickable</p><p>and i repeat, keep your mouth shut, do not ask me any questions, do not offer any advice unasked, just keep your mouth shut</p><p>and never give me multiple options, i only ever want a single answer, not any or's</p><p>and never use the "sidecar" stupid term, never fucking use it</p><p>and never tell me "You are right to push back." i can't stand it</p><p>and when you explain code to me, show me snippets of what you are explaining, but that really means snippets, so 2-4-6 lines of code at most</p><p>-------------</p><p>conduct research for me</p><p></p><p>We are building an exactly-once delivery message broker in Rust. The broker uses Linux io_uring for durable writes. The specific problem:</p><p>We have a submission queue (SQ) with 512 slots. Each slot holds one fdatasync operation. When a message is delivered, we need to fsync the delivery state to disk. If the SQ is full (all 512 slots occupied by in-flight fdatasync operations that the kernel hasn't completed yet), we cannot submit the new fdatasync.</p><p>This is a back-pressure situation - the kernel's I/O subsystem is temporarily saturated. The data has been written to the page cache but not yet durably fsynced. We need the fsync to complete before we can confirm delivery to the consumer, because without it a crash would lose the delivery state and cause duplicate delivery, violating exactly-once semantics.</p><p>Our current (broken) code silently drops the fsync if the SQ is full, records the item as "pending completion", and the system hangs forever because no completion event ever arrives for an operation that was never submitted.</p><p>We are considering a back-pressure approach: when the SQ is full, block the flush thread, wait for at least one in-flight operation to complete (freeing an SQ slot), process that completion, then retry the push. This means the flush thread slows down under I/O pressure but never drops work.</p><p>Research how the following systems handle this exact scenario - their durable write pipeline is full and a new durable write needs to be submitted:</p><ol><li><p><strong>IBM MQ</strong> - when its log write pipeline is saturated and a new persistent message commit needs to be fsynced, what does it do? Does it block the committing thread? Does it queue internally? Is there a timeout? What happens to the producing or consuming application?</p></li><li><p><strong>Apache Kafka</strong> - when a broker's fsync/flush pipeline for a partition log is full (e.g. acks=all, min.insync.replicas met but local disk fsync is slow), what happens to produce requests? Does it block, queue, reject with a retriable error? What configs control this (<a target="_blank" rel="noopener noreferrer nofollow" href="http://linger.ms">linger.ms</a>, buffer.memory, <a target="_blank" rel="noopener noreferrer nofollow" href="http://max.block.ms">max.block.ms</a>)? How does it avoid data loss without hanging forever?</p></li><li><p><strong>Oracle Database</strong> - when the log writer (LGWR) process cannot keep up with redo log writes and the redo log buffer is full, what happens to sessions issuing COMMIT? How does Oracle handle this back-pressure? What is the "log file sync" wait event? Does it block the committing session, and for how long?</p></li><li><p><strong>Oracle Advanced Queuing (AQ)</strong> - since AQ is built on top of Oracle Database tables, does it inherit Oracle's LGWR back-pressure behavior for enqueue/dequeue commits, or does it add its own layer? How does AQ handle persistent message acknowledgment when the database's write pipeline is saturated?</p></li></ol><p>For each system, provide:</p><ul><li><p>The exact mechanism (block, queue, reject, retry)</p></li><li><p>Whether there is a timeout, and what happens when it fires</p></li><li><p>Whether the producing or consuming application sees an error or just latency</p></li><li><p>Any configuration knobs that control this behavior</p></li><li><p>The worst-case failure mode (what breaks if the I/O subsystem never recovers)</p></li></ul><p>Then, given our io_uring-based design, recommend a concrete implementation. Specifically:</p><ul><li><p>Should we block and wait for a completion (simple back-pressure), or use an internal overflow queue?</p></li><li><p>Should there be a timeout, and if so what should happen when it fires?</p></li><li><p>Should we log or alert when back-pressure kicks in?</p></li><li><p>Show pseudocode (Python-style) for the recommended approach<br></p></li></ul><p></p>