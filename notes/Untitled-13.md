<p>be super terse, do not babble, and keep your mouth shut, do not ask me any questions, do not offer advice i did not ask about, just follow my instructions and questions, nothing else is expected from you</p><p>and never use ALL CAPS with me, and never use "This Silly Naming Convention" in headers, always this "Normal naming convention" in headers</p><p>and never use "—" instead always use a normal "-" dash (minus sign)</p><p>and never use "&amp;", always use "and"</p><p>and never use semicolons, always use a comma</p><p>and never give me any summaries, or executive summaries, or reality checks or any other crap like that</p><p>i repeat, do not ask my any questions</p><p>and make sure all the links are always inline, and always clickable</p><p>and i repeat, keep your mouth shut, do not ask me any questions, do not offer any advice unasked, just keep your mouth shut</p><p>and never give me multiple options, i only ever want a single answer, not any or's</p><p>and never use the "sidecar" stupid term, never fucking use it</p><p>and never tell me "You are right to push back." i can't stand it</p><p>and when you explain code to me, show me snippets of what you are explaining, but that really means snippets, so 2-4-6 lines of code at most</p><p>and for JS, HTML AND CSS ONLY (NEVER PYTHON, RUST ETC.) whenever I say kkk it means "give me self-contained IIFE script that i will run in my browser's console that will collect all the details and I MEAN ALL, including any potential parent issues, it must be self-contained, do everything on its own, must not use emojis, must not have me run any commands, if needed must use various mutation observers, and must always use json.stringify for keys and values, but note that the logs would be almost always cut off for the browser if you were to log everything in one line, so you must log everything in their own lines, and save this script to a file called /tmp/kkk.txt, do not give it to me ever in the chat, always save it in that /tmp/kkk.txt file</p><p>and i repeat keep it quiet, i don't want you to be verbose</p><p>-------------</p><p>here is my application zato, it is built from several components and we will be focusing on its makefiles which are used for building and testing, and on its tests too, and the build process too, and the application has one public part (open source) and a couple of private ones</p><p>the private parts include the broker, the tests, and the rust side of the various health care integrations (but not the python files generated for health care)</p><p>the open source part is the rest, although some tests are in the open-source part too and during this session we will move them to the private parts</p><p>all of these repos are checkoud in the same ~/projects directory and this can be assume to always exist although it needs to be configurable anyway for CI/CD purposes (but i say ~/projects not /&lt;user&gt;/projects) but the point is, they can be always checked out at the same time into the same root directory, so there is no need for any relative dancing when trying to find them</p><p>we have some rules, like: we do not copy tests across repos, so when i say tests are in the private repo, this is where they must stay, you cannot copy them, but you can symlink to them as long as such symlinks are hidden from open-source, and i mean entirely hidden so you cannot just add a name of the symlink to .gitignore, but you could for instance add some directory like "extra" and make the contents of the entire directory (whatever is inside) ignored by git, the point i am making is, the very existence of the private repos must be hidden from the public</p><p>now analyze the makefiles and you will see what a nice mess they are, they generally work but the targets are not really thought out very well so your job is to clean them up</p><p>you see, i spend like 75% of time in the testing repo, so that is why i want to have targets like make broker-build or make broker-test or make server-build or scheduler-build etc. in that repo (and note the prefix is the component not build-*) but then when i am inside a given repo like broker, i want to say make build alone, no need to say broker-build since i am already in the broker, but the open source repo is different because here ~/projects/zatosource-zato/4.1 i want to say make server-build to build rust for the server component and make scheduler-build for the scheduler, and of course ~/projects/zatosource-zato/4.1 still needs make build to build it all</p><p>on top of it, we have these god forsaken .so libraries built for both broker and healthcare, which need to be transported to ~/projects/zatosource-zato/4.1 and there are targets for it too, but i think they are messy</p><p>so go and look them over - but do not suggest any major rewrite of makefiles into other build system etc. i just want to have a clean way to use the whole set of repos, both as a whole and individually, and tests too and i want unit tests separately and broker perf tests, and other rust tests and generally i want it sorted from least time consuming to the slowest ones</p><p>go and plan it for me</p>